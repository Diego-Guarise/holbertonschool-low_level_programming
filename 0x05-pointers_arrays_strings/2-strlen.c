#include <stddef.h>
#include <string.h>
#include "holberton.h"

/**
 * _strlen - returns the length of a string.
 * return - strlen s
 * @s: variable
 */

int _strlen(char *s)
{
	return (strlen(s));
}
