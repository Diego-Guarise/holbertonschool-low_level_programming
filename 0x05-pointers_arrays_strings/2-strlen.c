#include <stddef.h>
#include <string.h>
#include "holberton.h"

/**
 * _strlen - returns the length of a string.
 * Return: returns the length of the string
 * @s: variable
 */

int _strlen(char *s)
{
	return (strlen(s));
}
