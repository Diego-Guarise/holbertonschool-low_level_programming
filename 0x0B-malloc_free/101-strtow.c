#include <stdio.h>
#include "holberton.h"
#include <stdlib.h>

/**
 * word_count - Count words separate by spaces in a string
 * @str: String to check
 *
 * Return: Number of words;
 */

int word_count(char *str)
{
	if (*str)
		printf("\n");
	return (0);
}
