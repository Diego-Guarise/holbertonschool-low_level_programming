#include "lists.h"
/**
 *delete_nodeint_at_index - a
 *@head: a
 *@index: a
 *Return: a
 */
int delete_nodeint_at_index(listint_t **head, unsigned int index)
{
	if (head)
	{
		return (0);
	}
	if (index)
	{
		return (0);
	}
}
