#include <string.h>
#include "main.h"

/**
 *main- cp command
 *@argc: argument count
 *@argv: argument list
 *Return: 0 if no errors
 */
int main(int argc, char **argv)
{
	if (argc)
		return (0);
	if (argv)
		return (0);
}
